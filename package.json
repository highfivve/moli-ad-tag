{
  "version": "1.20.1",
  "private": true,
  "workspaces": [
    "ad-tag",
    "moli-debugger",
    "modules/*",
    "examples/publisher"
  ],
  "scripts": {
    "build:prebid": "cd node_modules/prebid.js && npm install && node_modules/.bin/gulp build --modules=../../examples/prebid/modules.json"
  },
  "devDependencies": {
    "prebid.js": "2.30.0"
  }
}
