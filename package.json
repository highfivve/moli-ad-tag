{
  "version": "1.26.1",
  "private": true,
  "workspaces": [
    "ad-tag",
    "moli-debugger",
    "modules/*",
    "examples/*"
  ],
  "scripts": {
    "build:prebid": "cd node_modules/prebid.js && npm install && node_modules/.bin/gulp build --modules=../../examples/prebid/modules.json",
    "build:prebid-dev": "cd node_modules/prebid.js && npm install && node_modules/.bin/gulp build-bundle-dev --modules=../../examples/prebid/modules.json",
    "preversion": "yarn build:prebid && yarn workspaces run validate",
    "postversion": "git push && git push --tags"
  },
  "devDependencies": {
    "prebid.js": "2.34.0"
  }
}
