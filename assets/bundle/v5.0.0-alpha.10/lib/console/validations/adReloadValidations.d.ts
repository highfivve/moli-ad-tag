import type { AdSlot } from '../../types/moliConfig';
import { Message } from '../components/globalConfig';
import type { ModuleMeta } from '../../types/module';
export declare const checkAdReloadConfig: (messages: Message[], modules: ReadonlyArray<ModuleMeta>, slots: AdSlot[], labels: string[]) => void;
